// 자바스크립트는 다중패러다임언어이기 때문에 실행문과 반복문이 둘다된다
//보통함수
// const add1 = (a: number, b: number) => a + b;

// 고차함수
// const add2 =
//   (a: number): ((number: number) => number) =>
//   (b: number) =>
//     a + b;

//   //closure = 클로저

// const result = add2(1)(2);

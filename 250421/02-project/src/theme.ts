// 이컬러 테마 값을 가져다 쓸 수 있다
import { DefaultTheme } from "styled-components";

export const darkTheme = {
  textColor: "#f5f6fa",
  bgColor: "#2f3640",
  accentColor: "#4cd137",
};

export const lightTheme = {
  textColor: "#000",
  bgColor: "#fff",
  accentColor: "#9cd137",
};

// export default Theme;
